{% extends 'base.html.twig' %}

{% block title %}Offres d'emploi{% endblock %}

{% block body %}
    <div class="container py-10">

        <!-- Header -->
        <div class="card mb-10">
            <div class="card-body d-flex flex-column flex-md-row justify-content-between align-items-md-center">
                <div>
                    <h1 class="fs-2hx fw-bold text-dark mb-2">Offres d'emploi</h1>
                    <p class="text-gray-600 mb-0">Consultez les opportunités de carrière dans le groupe GDH.</p>
                </div>

                {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PRO') %}
                    <a href="{{ path('app_offre_new') }}" class="btn btn-primary mt-4 mt-md-0">
                        <i class="ki-duotone ki-plus fs-2"></i>
                        Créer une offre
                    </a>
                {% endif %}
            </div>
        </div>

        {% if offres is not empty %}
            <div class="row g-6">
                {% for offre in offres %}
                    <div class="col-md-6 col-xl-4">
                        <div class="card card-flush h-100 shadow-sm hover-elevate-up transition-all">
                            <div class="card-header border-0 pt-6">
                                <div class="d-flex justify-content-between align-items-start w-100">
                                    <div class="flex-grow-1">
                                        <h3 class="card-title">
                                            <span class="fw-bold fs-3 text-dark">
                                                {{ offre.mission|length > 40 ? offre.mission|slice(0,40) ~ '...' : offre.mission }}
                                            </span>
                                        </h3>
                                        <span class="badge badge-light-{{ offre.etat ? 'success' : 'secondary' }} mt-2">
                                            {{ offre.etat ? 'Actif' : 'Inactif' }}
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <div class="card-body py-5">
                                <!-- Type de contrat -->
                                <div class="d-flex align-items-center mb-3">
                                    <i class="ki-duotone ki-briefcase fs-2 text-primary me-2">
                                        <span class="path1"></span>
                                        <span class="path2"></span>
                                    </i>
                                    <span class="text-gray-800 fw-semibold">{{ offre.type }}</span>
                                </div>

                                <!-- Salaire -->
                                {% if offre.salaire %}
                                    <div class="d-flex align-items-center mb-3">
                                        <i class="ki-duotone ki-euro fs-2 text-success me-2">
                                            <span class="path1"></span>
                                            <span class="path2"></span>
                                            <span class="path3"></span>
                                        </i>
                                        <span class="text-gray-800">{{ offre.salaire }}€</span>
                                    </div>
                                {% endif %}

                                <!-- Description -->
                                <p class="text-gray-700 mt-4 mb-4">
                                    {{ offre.description|length > 100 ? offre.description|slice(0,100) ~ '...' : offre.description }}
                                </p>

                                <!-- Actions -->
                                <div class="d-flex gap-2">
                                    <a href="{{ path('app_offre_show', {'id': offre.id}) }}" class="btn btn-sm btn-light-primary flex-grow-1">
                                        <i class="ki-duotone ki-eye fs-2">
                                            <span class="path1"></span>
                                            <span class="path2"></span>
                                            <span class="path3"></span>
                                        </i>
                                        Voir
                                    </a>
                                    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_PRO') %}
                                        <a href="{{ path('app_offre_edit', {'id': offre.id}) }}" class="btn btn-sm btn-outline-primary">
                                            <i class="ki-duotone ki-pencil fs-2">
                                                <span class="path1"></span>
                                                <span class="path2"></span>
                                            </i>
                                        </a>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        {% else %}
            <div class="alert alert-info d-flex align-items-center p-5">
                <i class="ki-duotone ki-information-5 fs-2hx text-info me-4">
                    <span class="path1"></span>
                    <span class="path2"></span>
                    <span class="path3"></span>
                </i>
                <div class="d-flex flex-column">
                    <h4 class="mb-1 text-info">Aucune offre disponible</h4>
                    <span>Aucune offre d'emploi n'est actuellement publiée.</span>
                </div>
            </div>
        {% endif %}

    </div>
{% endblock %}
